name: dart_vips_example
description: Flutter desktop demo for dart_vips image processing.

publish_to: none

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dart_vips:
    path: ../..

flutter:
  uses-material-design: true
